/*
function min(a, b) {
  if (a < b) {
    console.log(a);
  } else {
    console.log(b);
  }
}
min();


function num (c) {
  console.log(c)
}

num();



function main(){
  min(5, 10);
  num(15);
}

main();

*/


function myFunc (arr, number) {
  const newArr = [];
  for (let i = 0; i < myFunc; i++) {
    newArr.push(arr, number);
  }
}

myFunc('a', 3);


/*function example(exam, projects) {
  if (exam > 90 || projects >10) { return 100;}
  else if (exam > 75 && projects > 5 ) { return 90;}
  else if (exam > 50 && projects > 2 ) { return 75;}
  else if (exam > 30 && projects > 1 ) { return 50;}
  else { return 0;}
}
console.log(example(100, 9));*/




/*
заполннить массив цикл (фор ай) и метод пуш (массивы данных)

условные конструкции и
логические оператор и или*/
